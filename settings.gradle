//Just builds the library. Nothing too fancy.

plugins {
	//Gradle threw a fit last time this was removed. I'm just going to keep it here for now.
	id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'Wheelie'
include('lib')
