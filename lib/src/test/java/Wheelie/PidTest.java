package Wheelie;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertations.*;

class PidTest {
	@Test void PidCalc () { //TODO: Get this to actually test instead of being a placeholder
		PID PidClass = new PidClass (0.0, 0.0, 0.0);
		assertTrue (true, "Return Statement");
	}
}
